version: '3.8'

services:
  app:
    build: .
    container_name: nestjs-app
    restart: always
    ports:
      - '3000:3000'
    env_file:
      - .env  # Load environment variables from the .env file
